'use strict';

const names = ['bob', 'jim', 'tom', 'jessica', 'mary']

const printListOfOlymians = (array) => {
    array.forEach((item) => {
        console.log(item);
        
    });

}
//comment 
printListOfOlymians(names);